# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ai-diagnostics-agent
metadata:
  template: aspire-starter@0.1.0

services:
  agent-api:
    project: ./src/Agent.Api
    host: containerapp
    
  agent-worker:
    project: ./src/Agent.Worker
    host: containerapp

# Azure resources provisioned via Aspire
infra:
  provider: bicep
  path: infra
